import { ToolFilterPipe } from './tool-filter.pipe';

describe('ToolFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
